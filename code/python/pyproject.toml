[project]
name = "aws-lifecycle-automation-python-tests"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.43.16",
    "pydantic>=2.11.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
